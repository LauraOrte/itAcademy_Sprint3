const dataJson = require('./parameters.json')

const calculator = {
    suma: (a, b) => a + b,
    resta: (a, b) => a - b,
    multiplicacio: (a, b) => a * b,
  };
  
  module.exports = calculator;
